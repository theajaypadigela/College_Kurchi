# Development / CI dependencies (install on top of requirements.txt).
-r requirements.txt

pytest>=8.0
mongomock>=4.1        # in-memory MongoDB for tests (no live mongod needed)
httpx>=0.27           # required by fastapi.testclient.TestClient
ruff>=0.5             # linter (used in CI)
